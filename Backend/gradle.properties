# Aktiviert den Configuration Cache für schnellere Build-Zeiten
org.gradle.configuration-cache=true

# Schaltet den Build-Cache ein
org.gradle.caching=true

# Ermöglicht parallele Projektausführung
org.gradle.parallel=true
